import 'package:flutter/material.dart';

class AuthByPhone extends StatelessWidget {
  const AuthByPhone({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('AuthByPhone'),
    );
  }
}
